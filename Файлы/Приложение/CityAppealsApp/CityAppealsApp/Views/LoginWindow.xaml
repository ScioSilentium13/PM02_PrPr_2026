<Window
    x:Class="CityAppealsApp.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:CityAppealsApp.ViewModels"
    Title="Вход в систему"
    Width="400"
    Height="480"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image
            Height="64"
            Margin="0,10"
            HorizontalAlignment="Center" />
        <TextBlock
            Grid.Row="1"
            Margin="0,5"
            HorizontalAlignment="Center"
            FontSize="16"
            FontWeight="Bold"
            Text="Администрация города Слободской" />

        <TextBlock
            Grid.Row="2"
            Margin="0,15,0,0"
            Text="Логин *:" />
        <TextBox
            Grid.Row="3"
            MaxLength="50"
            Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="4"
            Margin="0,15,0,0"
            Text="Пароль *:" />
        <PasswordBox
            Name="PasswordBox"
            Grid.Row="5"
            MaxLength="350" />

        <StackPanel
            Grid.Row="6"
            Margin="0,20"
            HorizontalAlignment="Center"
            Orientation="Vertical">
            <Button
                Height="35"
                Margin="5"
                Click="LoginButton_Click"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox}"
                Content="Войти" />
            <Button
                Height="35"
                Margin="5"
                Command="{Binding RegisterCommand}"
                Content="Регистрация" />
        </StackPanel>
    </Grid>
</Window>